<div class="pt-4" id="<%= dom_id poly_note %>">
  <div class="px-3 p-1 mr-20 bg-amber-50 text-sm leading-5 text-gray-800 rounded">
    <p class="pt-2"><%= poly_note.note  %></p>
    <div class="text-xs">
      
    </div>
    <div class="text-xs pt-6 text-slate-500">
      <p>Note by <%= model(poly_note) %>: <%= model_name(poly_note) %></p>
      <p>Created at <%= poly_note.created_at %></p>
    </div>
  </div>
</div>
